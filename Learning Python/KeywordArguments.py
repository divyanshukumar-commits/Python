def increment(number, by):
    return number + by


# result = increment(2, 1)
# print(result)
# Temperory stores result in a variable and passes into print function
print(increment(number=2, by=1))  # keyword argument
